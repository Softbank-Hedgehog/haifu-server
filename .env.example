# ============================================
# hAIfu Server 환경변수 템플릿
# ============================================
#
# 사용법:
# 1. 이 파일을 복사: cp .env.example .env
# 2. 실제 값으로 수정
# 3. .env 파일은 .gitignore에 포함되어 Git에 올라가지 않음
#
# 자세한 설명은 팀 노션 ".Env 페이지" 참고
# ============================================

# ============================================
# 환경 구분
# ============================================
ENVIRONMENT=local
# - local: 로컬 개발 환경
# - production: AWS Lambda 프로덕션 환경

# ============================================
# GitHub OAuth (필수)
# ============================================
GITHUB_CLIENT_ID=
# GitHub OAuth App Client ID
# 발급 방법: https://github.com/settings/developers

GITHUB_CLIENT_SECRET=
# GitHub OAuth App Client Secret
# 주의: 절대 공개 저장소에 올리지 말 것!

# ============================================
# JWT 인증 (필수)
# ============================================
JWT_SECRET_KEY=
# JWT 토큰 서명용 비밀키
# 랜덤 문자열 생성 (최소 32자 권장)
# 예시: openssl rand -hex 32

JWT_ALGORITHM=HS256
# JWT 알고리즘 (기본값 사용 권장)

JWT_EXPIRE_DAYS=7
# JWT 토큰 만료 기간 (일)

# ============================================
# Frontend URL
# ============================================
FRONTEND_URL=http://localhost:3000
# 프론트엔드 URL (CORS 및 OAuth 콜백용)

# ============================================
# AWS 설정
# ============================================
AWS_REGION=ap-northeast-2
# AWS 리전 (서울)

# ============================================
# DynamoDB (로컬 개발용)
# ============================================
DYNAMODB_ENDPOINT=http://localhost:8000
# 로컬 개발: http://localhost:8000
# 프로덕션: 비워둠 (AWS DynamoDB 자동 사용)

DYNAMODB_PROJECTS_TABLE=haifu-projects
# Projects 테이블명

DYNAMODB_SERVICES_TABLE=haifu-services
# Services 테이블명

# ============================================
# 로컬 개발 시 AWS 자격증명 (DynamoDB Local용)
# ============================================
AWS_ACCESS_KEY_ID=dummy
# 로컬 DynamoDB Local 사용 시 더미 값

AWS_SECRET_ACCESS_KEY=dummy
# 로컬 DynamoDB Local 사용 시 더미 값
